# DEC-006 Issue/Epic mapping is the Dev/QA source

The Excel v7.1 admin workbook exposes Dev SP, QA SP and PD columns on Issue and Epic mapping tables. Automated estimates now read that split from configuration (scaled if selected SP differs from the mapped total) instead of the earlier complexity-score heuristic.
